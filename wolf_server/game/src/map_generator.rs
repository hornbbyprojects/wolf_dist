use crate::game::*;

//how should this work?
//generate locally in blocks?
//generate globally?
//generate global structure, then apply detail?
pub fn generate_map(){

}

struct MapData{

}


//problem: squares are stored in chunks.
//generate sand chunks, overlay upon them?
//requires proper chunk saving system.
//or a way to force chunks to remain loaded.
pub fn generate_room(top_left_coords: SquareCoords, width: u32, height: u32){
    walls around the edge 

}